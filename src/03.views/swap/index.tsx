import { SwapForm } from "@/05.features/swap-form/ui";

const SwapView = () => {
  return (
    <main className="flex min-h-[calc(100vh-80px)] items-center justify-center bg-white p-4">
      <SwapForm />
    </main>
  );
};

export default SwapView;